// vendors
import styled from 'styled-components';

// locals
import { colors, metrics } from '../../../styles';

export const SecondaryTitleStyle = styled.h2`
  color: ${colors.dark};
  font-size: ${metrics.fontBase};
  margin-bottom: ${metrics.baseMargin}px;
`;
